import AbstractInput, {component, layout} from "gold-entity/form/abstract-input";
import CSelect from "./select.svelte"


interface IOption {
	label: string,
	value: any
}

@component(CSelect)
@layout("row")
export default class SelectInput extends AbstractInput {

	public options: Array<IOption> = [];

	Options(options: Array<IOption>): this {
		this.options = options;
		return this;
	}
}
























































































































































































































































































































































































































































































































































































































































